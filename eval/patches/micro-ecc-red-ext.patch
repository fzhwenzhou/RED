RED eval: whole 256x256 multiply from eval/spec/micro-ecc.json.
Applied only to build copies; includes operand marshalling and a size fallback.

--- a/uECC.c
+++ b/uECC.c
@@ -2,6 +2,7 @@

 #include "uECC.h"
 #include "uECC_vli.h"
+#include "red_ext.h"

 #ifndef uECC_RNG_MAX_TRIES
     #define uECC_RNG_MAX_TRIES 64
@@ -417,6 +418,18 @@
                                 const uECC_word_t *left,
                                 const uECC_word_t *right,
                                 wordcount_t num_words) {
+    /* RED: the pinned instruction accepts exactly two eight-limb operands. */
+#if uECC_WORD_SIZE == 4
+    if (num_words == 8) {
+        uint32_t operands[16];
+        for (wordcount_t i = 0; i < 8; ++i) {
+            operands[i] = left[i];
+            operands[8 + i] = right[i];
+        }
+        red_mult256(operands, result);
+        return;
+    }
+#endif
     uECC_word_t r0 = 0;
     uECC_word_t r1 = 0;
     uECC_word_t r2 = 0;
